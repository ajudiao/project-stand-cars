<!DOCTYPE html>
<html lang="pt">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Recuperar Senha - Saeld Auto</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #f4f1ec;
            font-family: Arial, Helvetica, sans-serif;
        }

        .forgot-password-container {
            max-width: 900px;
            width: 100%;
            background: #fff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.12);
        }

        .car-side {
            background: url("https://images.unsplash.com/photo-1503376780353-7e6692767b70") center/cover no-repeat;
            min-height: 500px;
        }

        .logo {
            font-size: 26px;
            font-weight: 600;
        }

        .input-group-text {
            background: #f1f3f5;
            border-right: 0;
        }

        .form-control {
            border-left: 0;
        }

        .btn-dark {
            padding: 10px;
        }
    </style>

</head>

<body>

    <div class="forgot-password-container">

        <div class="row g-0">

            <!-- FORM -->
            <div class="col-md-6 p-5 d-flex flex-column justify-content-center">

                <div class="mb-4">
                    <h3 class="logo">
                        <i class="bi bi-car-front-fill"></i> Saeld Auto
                    </h3>
                    <p class="text-muted">Recuperar Senha</p>
                </div>

                <div class="alert alert-info mb-4" role="alert">
                    <i class="bi bi-info-circle"></i>
                    Insira o email associado à sua conta para receber um link de recuperação de senha.
                </div>

                <form action="/admin/forgot-password" method="POST">

                    <div class="mb-3">
                        <label class="form-label">Email</label>

                        <div class="input-group">
                            <span class="input-group-text">
                                <i class="bi bi-envelope"></i>
                            </span>

                            <input type="email" class="form-control" name="email" placeholder="Digite o seu email" required>
                        </div>

                    </div>

                    {% if error %}
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="bi bi-exclamation-triangle"></i> {{ error }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endif %}

                    {% if success %}
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <i class="bi bi-check-circle"></i> {{ success }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endif %}

                    <button class="btn btn-dark w-100 mb-3">
                        <i class="bi bi-send"></i> Enviar Link de Recuperação
                    </button>

                    <p class="text-center text-muted small">
                        Lembrou a senha? <a href="/admin/login" class="text-decoration-none">Faça login aqui</a>
                    </p>

                </form>

            </div>

            <!-- IMAGEM -->
            <div class="col-md-6 car-side d-none d-md-block"></div>

        </div>

    </div>

</body>

</html>
